## Interface of the Gregorian and Time Mixin

Datetime, globals used
	ENV['language'] < $language < Thread[:language] < #language = 
	EVN['timezone'] < $timezone < Thread[:timezone] < #timezone = 

Datetime, class methods:
	civil(year, month, day)
	commercial(year, week, day_of_week)
	ordinal(year, day_of_year)
	parse(*formats)
	today
	now
	time(hour, minute, second)
	epoch(unix_epoch_stamp)

	leap?(year)

Datetime, instance methods:
	at(hour, minute, second)
	in(zone, language)
	change_zone(zone, language)

	<=>(datetime)
	+(duration)
	-(duration_or_datetime)
	
	succeeding(unit, step, upper_limit, &block)
	current(unit, at, &block)
	previous(unit, step, lower_limit, &block)

	leap_year?

	civil_year (alias year)
	commercial_year
	month
	iso_8601_week (alias week)
	day_of_week
	day_of_month (alias day)
	day_of_year
	hour
	minute
	second
	fraction
	usec
	offset
	timezone # ISO 639-1? also see ISO 3166
	language
	
	strip_date
	strip_time
	
	datetime?
	date?
	time?
	date_only?
	time_only?

	month_name(language)
	day_name(language)
	format
	strftime
	
	to_time
	to_hash
	to_a

# FORMAT
	uppercase: (generally) time
	lowercase: (generally) date

	%a -> monthname, %3a -> first 3 letters of the name, %-3a -> last 3 letters
	%b -> dayname, %3b -> first 3 letters of the name
	%y -> year, 4 digits, (%-2y -> last 2 digits, %2y -> first 2 digits) 0000..9999
	%m -> month, 1 or 2 digits, (%02m -> always 2 digits, 0 padded, %2m -> space padded) 01..12
	%d -> day of month (same as %m) 01..31
	%j -> day of year (same as %m) 001..366
	%k -> day of week, 0=monday, 6=sunday, %+k -> 1..7, %2k -> 2=monday
	%w -> week of year (iso 8601) 01..53
	
	%H -> hour of day (same as %m), 00..23
	%I -> hour of day (same as %m), 01..12
	%M -> minute of hour (same as %m) 00..59
	%S -> second of minute (same as %m) 00..59
	%O -> offset in format ±HHMM
	%Z -> timezone
	%P -> meridian indicator (AM/PM)
	
	%% - Literal % character
	
	# strftime formats with no analogous #format-directive:
	%c - The preferred local date and time representation
	%U - Week  number  of the current year,
			 starting with the first Sunday as the first
			 day of the first week (00..53)
	%x - Preferred representation for the date alone, no time
	%X - Preferred representation for the time alone, no date

NOTES:
	timezone= removed, would render Datetime mutable
	language= removed, would render Datetime mutable
